interface:
  display_name: "Build Hours"
  short_description: "Repo-specific patterns and workflows for build-hours"
  default_prompt: "Use the build-hours repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true